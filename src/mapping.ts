// These functions are the mappings defined in subgraph.yaml
// Re-export them so we can keep them organized in separate files
export { handleTransferEvent } from './handle-transfer-event'
export { handleBlock } from './handle-block'
